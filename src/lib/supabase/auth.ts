// DEPRECATED — all imports should use @/lib/auth/server instead.
// This file is kept as an empty re-export stub so any stray imports
// don't break the TypeScript build during migration.
export { resolveAuthContextFromToken, saveUserProviderSettings, getSuperAdminEmail } from '@/lib/auth/server'
export type { AuthContext } from '@/lib/auth/server'
