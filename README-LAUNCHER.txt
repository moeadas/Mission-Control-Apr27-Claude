Mission Control packaged runtime folder
Path:
This folder is self-contained and can be moved or copied to another machine.

Use:
- Start Mission Control.app
- Stop Mission Control.app

The launcher starts the production server on http://localhost:3000
Logs are written to:
~/Library/Logs/MissionControl/server.log

This folder is now the clean packaged app folder to keep working from.
