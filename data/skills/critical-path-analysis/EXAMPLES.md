## Example 1

Input:
Run critical path analysis on a 5-week campaign production project. Tasks: brief (1d), concept (3d), design (5d), copy (3d), review (2d), amends (2d), final (1d). Design and copy can run in parallel after concept. Review follows both.

Output:
Critical path analysis showing the critical path, float for non-critical tasks, and compression options
